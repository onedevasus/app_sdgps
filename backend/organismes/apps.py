from django.apps import AppConfig


class OrganismesConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'organismes'
    verbose_name = "Organismes (premier / deuxième niveau)"
