# Données runtime — persistées via des volumes Docker, jamais embarquées dans l'image
db.sqlite3
media/

# Secrets d'environnement — fournis au runtime via docker-compose (env_file), jamais dans
# l'image. Les templates *.example (non sensibles) restent inclus.
.env
.env.local
.env.development
.env.production

# Artefacts Python / venv / caches
__pycache__/
*.py[cod]
.venv/
venv/
env/
.pytest_cache/
.mypy_cache/

# Divers
*.log
.git/
.idea/
